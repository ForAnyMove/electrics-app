import { Text, View } from "react-native";

export default function Tab1() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Readings you have created and found a suitable employee now what remains is to make contact and start working together</Text>
    </View>
  );
}